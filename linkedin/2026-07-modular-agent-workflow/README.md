# GPT Pro Handoff: Modular AI Agent Workflow Post

Use this folder as the context packet for drafting the updated LinkedIn post.

## Files

- `GENERALIZED_WORKFLOW_INSIGHTS.md`: the transferable engineering lessons.
- `OLD_TO_NEW_COMPARISON.md`: the before/after narrative for the post.
- `GPT_PRO_LINKEDIN_PROMPT.md`: the prompt to give GPT Pro.

## Post Angle

The revised post should not just say "I use Codex with GitHub and AWS." The sharper point is:

> My first AI-agent workflow had a real flaw: production deployment and cloud resources were too bundled. The better pattern is to make resource ownership explicit, split deploy targets, and let agents route checks and deploys from changed paths.

Keep the post general. Do not mention private project names, domains, account IDs, emails, tokens, secrets, or customer/product-specific implementation details.
